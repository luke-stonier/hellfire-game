{
  "name": "content-builder",
  "private": true,
  "scripts": {
    "init": "dotnet tool install -g dotnet-mgcb && npm i -g",
    "build-content": "node ./build-content.mjs",
    "deploy": "node ftp-upload.mjs ./ftp-package.json"
  },
  "dependencies": {
    "basic-ftp": "^5.1.0"
  }
}
